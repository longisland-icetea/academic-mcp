# Convenience copy of the runtime dependencies. The source of truth is
# pyproject.toml; this file exists for people who install without a build
# backend (pip install -r requirements.txt).
mcp>=2.0
httpx>=0.27
ddgs>=9.0
python-docx>=1.1
jinja2>=3.1
# PINNED on purpose: camoufox's browser layout is version-specific and an
# unpinned upgrade invalidates the downloaded browser. Bump together with a
# fresh `camoufox fetch`, never on its own.
camoufox[geoip]==0.5.5
playwright>=1.45
